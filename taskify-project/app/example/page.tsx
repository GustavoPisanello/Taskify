/*Here we have a pagination route. In next JS it works automatically, if it is an app children folder, it will became a route. */

const Page = () => {
    return (
        <>
        <div>Example page</div>
        </>
    );
}

export default Page;